while(l<r)
           {
               if(arr)
               if(abs(arr[l]+arr[r])<=abs(ans)) 
               {
                   ans=arr[l]+arr[r];
               }
               if(arr[l]+arr[r]<0) ++l;
               else --r;
               
           }
